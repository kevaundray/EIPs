---
eip: 8365
title: Disallow New BLS Withdrawal Credentials
description: Reject deposits that would create new validators with 0x00 withdrawal credentials, leaving existing validators untouched
author: NC (@ensi321)
discussions-to: https://ethereum-magicians.org/t/eip-8365-bls-withdrawal-credential-retirement/29284
status: Draft
type: Standards Track
category: Core
created: 2026-07-18
requires: 6110, 7251, 7732
---

## Abstract

This EIP closes the inflow of the `BLS_WITHDRAWAL_PREFIX` (`0x00`) withdrawal credential type: deposits that would create new validators with `0x00` credentials are no longer processed. Existing `0x00` validators are entirely unaffected — they continue to validate, earn rewards, and accrue penalties exactly as before, and the `BLSToExecutionChange` operation remains available to rotate to `0x01` execution credentials at any time.

This is the first stage of a staged deprecation. With the inflow closed, the `0x00` population can only shrink. Companion EIPs address the existing population ahead of the post-quantum transition, and a final-stage EIP at the post-quantum fork removes the remaining `0x00` machinery (`BLSToExecutionChange`, its gossip topic and operation pool) from the consensus layer entirely.

## Motivation

The `0x00` credential type is the original genesis-era withdrawal format: `0x00 || sha256(withdrawal_bls_pubkey)[1:]`, with withdrawal controlled by a BLS key and no execution address. Since Capella opened one-way conversion to `0x01`, the population has fallen from roughly 600,000 to 9,290 validators (mainnet, July 2026: 9,119 active plus 171 exited awaiting credential rotation), but the decline has stalled. Conversion inclusions have decayed to double digits per month, and of the 9,119 still-active `0x00` validators, 374 have not attested for over six months and 253 have never attested in a 14-month observation window. These are, with high likelihood, validators whose keys are lost.

While the inflow remains open, this population can grow as well as shrink, and every addition compounds costs that grow with time rather than diminish:

- **Validators without an exit path.** No party can exit a `0x00` validator with a lost key. Voluntary exit requires the signing key, and execution-layer triggerable exits ([EIP-7002](./eip-7002.md)) require an execution address that `0x00` credentials do not have. At measured penalty rates (~0.55 ETH/year), natural ejection at `EJECTION_BALANCE` takes roughly 28 years. Offline rates confirm the asymmetry: 4.45% of active `0x00` validators have been offline for 30+ days, versus 0.10% for `0x01` and 0.05% for `0x02`. Every newly created `0x00` validator risks joining this population.
- **Permanent protocol surface.** While any `0x00` validator exists, every fork must carry `process_bls_to_execution_change`, its gossip topic, its operation pool, and the `0x00` branch of every credential check. An open inflow means this surface has no guaranteed end date.
- **Post-quantum transition burden.** BLS signatures are not post-quantum secure. The post-quantum roadmap has validators registering post-quantum keys ahead of a signature-switch fork, but validators that cannot act (lost keys) cannot register. `BLSToExecutionChange` itself becomes insecure once BLS is broken: the message reveals the withdrawal public key, allowing a quantum adversary to derive the key and race a competing change. Any plan that carries `0x00` validators into the post-quantum era must design and maintain hardened credential-change machinery indefinitely, solely for this population. Closing the inflow bounds the population that any such plan must account for.

This EIP closes the inflow and does nothing else. It deliberately does not exit, penalize, or otherwise touch any existing `0x00` validator. Active `0x00` validators continue to validate, and the EIP does not move or reduce their balances. Making the `0x00` population monotonically decreasing is a prerequisite for each subsequent deprecation stage. The staged path (close the inflow, then address the existing population, then remove the machinery) allows each stage to be evaluated separately.

## Specification

### Consensus layer

#### Deposit processing

`apply_pending_deposit` is modified so that deposits which would create a new validator with `0x00` withdrawal credentials are not applied:

```python
def apply_pending_deposit(state: BeaconState, deposit: PendingDeposit) -> None:
    validator_pubkeys = [v.pubkey for v in state.validators]
    if deposit.pubkey not in validator_pubkeys:
        # [New in this EIP] Do not create validators with BLS withdrawal credentials
        if deposit.withdrawal_credentials[:1] == BLS_WITHDRAWAL_PREFIX:
            return
        # Verify the deposit signature (proof of possession)
        if is_valid_deposit_signature(
            deposit.pubkey,
            deposit.withdrawal_credentials,
            deposit.amount,
            deposit.signature,
        ):
            add_validator_to_registry(
                state,
                deposit.pubkey,
                deposit.withdrawal_credentials,
                deposit.amount,
            )
    else:
        # Top-ups are unaffected
        index = ValidatorIndex(validator_pubkeys.index(deposit.pubkey))
        increase_balance(state, index, deposit.amount)
```

Rejected deposits receive the same treatment as deposits with invalid signatures. They are silently skipped, and the deposited ETH is not recoverable. Top-ups to existing validators, including `0x00` validators, are processed normally.

The guard applies when a pending deposit is processed, not when it is submitted. Fork activation does not grandfather pending deposits. Thus, a pre-fork `0x00` deposit that would create a validator is rejected if it is processed after activation. The deposited ETH remains unrecoverable.

#### Unchanged operations

No other state transition logic is modified. In particular, existing `0x00` validators are not exited, excluded from duties, or altered in any way.

`process_bls_to_execution_change` is unchanged and remains available. A `0x00` validator that rotates to `0x01` credentials gains an execution address and, once exited and past its `withdrawable_epoch`, has its balance transferred by the standard withdrawal sweep.

`process_voluntary_exit` is unchanged.

## Rationale

### Deposit guard only

The narrowest possible first stage of a deprecation is to stop new instances from being created. A deposit guard makes the `0x00` population monotonically decreasing. It does not change the duties, rewards, penalties, exits, or balances of existing validators. It also does not consume activation or exit churn. Companion EIPs can address the existing population separately.

### Deposit rejection semantics

Invalid-signature deposits are silently skipped while their ETH remains in the deposit contract ([EIP-6110](./eip-6110.md)). [EIP-8205](./eip-8205.md) uses the same permanent-rejection behavior for credential mismatches. Deposit tools have defaulted to execution credentials since Capella, so new deposits should not use `0x00`. Depositors must stop submitting `0x00` deposits before fork activation because the guard runs during pending-deposit processing.

### Staged deprecation precedent

The `SELFDESTRUCT` deprecation followed the same arc across multiple EIPs and forks: [EIP-6049](./eip-6049.md) (deprecation notice), [EIP-6780](./eip-6780.md) (behavioral restriction), and [EIP-4758](./eip-4758.md) (full deactivation). Closing new `0x00` creation is the analogous behavioral restriction for a credential type: maintaining legacy functionality indefinitely carries a cost for every subsequent upgrade, and a clear deprecation schedule serves both the network and the remaining users better than an open-ended wait.

## Backwards Compatibility

This EIP introduces backward-incompatible changes to the consensus-layer state transition and must be scheduled with a hard fork. No execution-layer changes are required. Existing deposit tooling is unaffected unless it creates new validators with `0x00` credentials. A pre-fork `0x00` deposit can lose funds if it remains pending at activation.

## Test Cases

The following cases define the required behavior:

- A valid `0x00` pending deposit for an unknown public key is skipped.
- An invalidly signed `0x00` pending deposit for an unknown public key is skipped.
- A top-up to an existing `0x00` validator is applied.
- Valid deposits that create `0x01` or `0x02` validators are applied.
- A valid `0x03` deposit follows the builder-routing rules in [EIP-7732](./eip-7732.md).
- A pre-fork `0x00` deposit is skipped if it is still pending when this EIP activates.
- Existing `0x00` validators remain unchanged.

## Security Considerations

### No change to existing validators or fund ownership

This EIP moves no balances and modifies no existing validator. Every `0x00` validator's balance, duties, and credential-rotation path are exactly as before. The population unable to rotate credentials (lost withdrawal keys) is unable to do so today, and this EIP does not change their position.

### Deposit contract griefing

The guard runs before deposit-signature verification. Thus, a depositor can submit an invalidly signed `0x00` deposit and lose the deposited ETH. The guard creates no validator entry, changes no existing validator, and cannot move another party's funds.

## Copyright

Copyright and related rights waived via [CC0](../LICENSE.md).
